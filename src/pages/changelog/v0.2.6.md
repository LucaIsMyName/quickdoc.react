# v0.2.6

## New Features

### MDX Component Library
- **Added 7 reusable MDX components** for enhanced documentation
  - `IFrame`: Embed YouTube videos, maps, and external content with responsive aspect ratios
  - `Callout`: Highlight info, warnings, success, and error messages with icons
  - `Tabs`: Organize related content in tabbed interfaces
  - `CodeBlock`: Enhanced code display with copy button and optional line numbers
  - `Steps`: Visual step-by-step guides with numbered circles
  - `Card`: Content containers with multiple variants (default, bordered, elevated)
  - `Badge`: Labels and tags with multiple colors and sizes

### Theme Integration
- **All components are fully theme-aware**
  - Automatically adapt to `config.theme.border.radius` settings
  - Respect `config.theme.border.size` configuration
  - Use theme color scheme (accent, light, dark)
  - Full dark mode support
- **New CSS variables**: `--theme-border-radius` and `--theme-border-size`
- **New CSS classes**: `theme-border-radius`, `theme-border-size`

### Vite Plugin Enhancement
- **Added HTTP middleware** to Vite plugin for proper MDX raw content loading
- Fixed path resolution for `/src/pages/` prefix in index.mdx files
- Improved raw content extraction for all MDX files

## Bug Fixes

### MDX Loading
- Fixed index.mdx files not displaying H1 in tab navigation
- Fixed index.mdx files not showing sidebar sub-headlines
- Resolved raw content loading issues for index files
- Fixed middleware path resolution for proper content extraction

### Component Styling
- Removed redundant borders and backgrounds from CodeBlock when used inside Tabs
- Reduced padding in Tabs component for tighter spacing
- Fixed line break rendering in CodeBlock component
- Added proper whitespace preservation with `white-space: pre`
- Fixed line numbers layout in CodeBlock with proper table structure

## Documentation

- Created `COMPONENTS.md` with comprehensive component documentation
- Added live examples in `src/pages/quickdoc/04-components.mdx`
- Documented all component props and theme integration
- Added usage examples for all components

## Technical Improvements

- Updated `colorStyles.ts` to support border radius and size configuration
- Enhanced CodeBlock with inline styles for proper whitespace handling
- Improved component accessibility and responsiveness
- All components follow theme configuration standards

## Search Improvements

### Weighted Search Results
- **Intelligent ranking** based on match location and heading level
  - H1 matches weighted highest (6x)
  - H2-H6 matches weighted progressively (5x-1x)
  - Content matches weighted lowest (0.5x)
- Results now prioritize heading matches over content matches

### Enhanced Visual Feedback
- **Accent color highlights** instead of hardcoded yellow
- Matching words use theme accent color with bold weight
- Cleaner, more consistent with app theme

### Improved Result Display
- **Highlighted titles** show matches directly in the heading
- Content matches shown separately below title
- No duplicate text - title matches only appear once
- Better visual hierarchy and readability

### Keyboard Shortcuts
- **Cmd+K / Ctrl+K** to open search dialog
- Overrides browser default search
- Faster access to documentation search
