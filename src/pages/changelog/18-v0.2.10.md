# v0.2.10

## New Features

 - Navigating to a section via URL hash or sidebar now briefly highlights the target heading using the current theme colors so users can see where navigation landed.

## Bug Fixes

 - The export options popover is rendered via a portal with a high stacking order so it stays above page-level blur overlays on mobile.